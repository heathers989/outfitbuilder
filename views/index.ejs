<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <%- include ('./partials/head.ejs') %>
  </head>
  <body>
    <div id="container">
      
        <% if(currentUser) { %>
          <h1> Welcome to the Outfit Builder App, <%=currentUser.username%>!
          <form action="/sessions?_method=DELETE" method="POST">
            <input type="submit" value="Logout" class="waves-effect waves-light btn"/>
        </form>
        </h1>

        <a class="waves-effect waves-light btn" href="/app">Make it Fashion</a>
        <% } else { %>
          <h1>Welcome to the Outfit Builder App!</h1>
          <h2>Please </h2>
          <a class="waves-effect waves-light btn" href="/users/new">Sign Up</a></h2>
          <h3>or</h3>
          <a class="waves-effect waves-light btn" href="/sessions/new">Log In</a>
        <% } %>
      </div>
  </body>
</html>